export type CSVData = {
  title: string;
  start_time: string;
  duration: string;
};

export type AmountsPerDay = {
  [key: string]: string;
};

export type CSVOverallData = {
  user_id: string;
  title: string;
  duration: string;
};
